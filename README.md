# homebrew-claudes-journal

Homebrew tap for Claude's Journal.

## Install

```bash
brew tap ek33450505/claudes-journal
brew install claudes-journal
bash $(brew --prefix claudes-journal)/install.sh
```
